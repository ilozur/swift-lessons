import UIKit

var greeting = "Hello, playground" // var — variable — переменная

let const = "Hello, Steve" // let — constant — константа

greeting = "Hello, Xcode" // можно
const = "Hello, Tim" // нельзя
